# ExDataHub

ExDataHub 是一个用 **Python** 编写的、面向量化交易和数据分析的 **多交易所行情数据中台 / 网关**。

> 目标：**用一套统一接口，对接多家交易所（OKX、Binance、Bybit...）的行情与衍生品数据**，让策略层不用关心每家交易所各自奇怪的 API 细节。

---

## ✨ 特性（规划）

- ✅ 使用 Python 实现，便于与量化策略、回测框架集成  
- ✅ 统一的数据模型：Kline / Ticker / OrderBook / FundingRate 等  
- ✅ 各交易所独立适配：OKX / Binance / Bybit（可扩展）  
- ✅ 支持 REST 公共接口（K 线、ticker、深度、资金费率、标记价等）  
- ✅ 服务层统一入口：`services.market.klines(...)` 这种调用方式  
- ✅ 提供简单的命令行工具（拉数据存 CSV 方便手工分析）  
- 🔜 支持私有接口：账户 / 持仓 / 下单 / 风控  
- 🔜 支持 WebSocket 实时行情聚合  
- 🔜 简单策略/信号模块（可选）

---

## 🧱 架构设计概述

整体设计理念：

1. **面向交易所：适配层（exchanges）**  
   每家交易所实现相同的 `ExchangeClient` 抽象接口，负责处理：
   - URL、请求签名、节流、限频等
   - 把原始 JSON 转换成统一的数据模型对象

2. **面向使用者：服务层（services）**  
   - 提供统一的高层函数，如：`klines(...)`、`ticker(...)`、`funding_rate(...)`  
   - 使用者（策略、脚本、研究）只调用 services，不直接依赖具体交易所实现

3. **核心模块（core）**  
   - 定义统一的数据模型（`Kline`, `Ticker`, `OrderBook` 等）  
   - 提供公共异常类型、HTTP 封装等

4. **工具模块（utils）**  
   - 日志、时间处理、数据字段转换（不同交易所的 symbol、interval 统一映射）

5. **配置模块（config）**  
   - 读取环境变量 / 配置文件（API Key、代理、默认参数等）

> 换句话说：  
> **策略层 只看到：services + 统一模型**，  
> **交易所差异 全部藏在 exchanges 下面**。

---

## 📁 目录结构（建议）

以下是一个推荐的基础目录结构，你可以根据需要调整：

```text
exdatahub/
  __init__.py

  config/
    __init__.py
    settings.py        # 读取配置、API Key、代理设置等

  core/
    __init__.py
    models.py          # 通用数据模型：Kline, Ticker, OrderBook, FundingRate
    exceptions.py      # 自定义异常
    http.py            # 统一 HTTP 封装，支持重试、超时、代理等

  exchanges/
    __init__.py
    base.py            # ExchangeClient 抽象基类
    okx_client.py      # OKX 实现
    binance_client.py  # Binance 实现（可选）
    bybit_client.py    # Bybit 实现（可选）

  services/
    __init__.py
    market.py          # 行情相关：klines / ticker / orderbook 等
    funding.py         # 资金费率、利率
    index_price.py     # 指数价、标记价
    # account.py       #（未来：私有接口）

  utils/
    __init__.py
    time_utils.py
    logger.py
    converters.py      # symbol / interval / 字段命名转换工具

  cli/
    __init__.py
    main.py            # 命令行入口：python -m exdatahub ...

tests/
  test_okx_client.py
  test_market_service.py

README.md
requirements.txt
pyproject.toml / setup.py
```
